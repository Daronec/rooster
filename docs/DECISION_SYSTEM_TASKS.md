# Задачи: система принятия решений

Чеклист по документу [DECISION_SYSTEM_IMPLEMENTATION.md](./DECISION_SYSTEM_IMPLEMENTATION.md). Отмечайте пункты по мере выполнения.

---

## Фаза 1 — Домен и тесты (без UI)

### Модель и типы

- [x] **D1-01** Зафиксировать правило: `TaskStatusEntity` = выполнение, `TaskFeasibilityStatus` = доступность сейчас (кратко в комментарии к entity / в `ARCHITECTURE.md` при необходимости).
- [x] **D1-02** Добавить enum `TaskFeasibilityStatus` (`blockedMoney`, `blockedMaterials`, `blockedDependencies`, `ready`, опционально `inProgress`, согласовать `done` с `completed`).
- [x] **D1-03** Ввести value object / record для структурированной **причины блокировки** (тип + данные для i18n: сумма нехватки, id материалов, id зависимых задач).
- [x] **D1-04** Расширить `TaskEntity`: `importance`, `urgency`, `complexity` (1–5), `estimatedCost`, `materialRequirementIds`, `dependencyTaskIds`; при раздувании — вынести профиль в `TaskDecisionProfile` (см. риски в implementation).
- [x] **D1-05** Обновить `copyWith` / равенство / `toString` при необходимости.
- [x] **D1-06** Реализовать `TaskDependencyValidator`: нет циклов, нет self-dependency, все id существуют (контракт входа: карта задач или репозиторий-абстракция на уровне use case).

### Движок скоринга и статуса

- [x] **D1-07** Вынести веса формулы (`wI`, `wU`, `wC`, …) в именованные константы или объект политики `FeasibilityScoringPolicy` (интерфейс + дефолтная реализация).
- [x] **D1-08** Реализовать чистые функции `f` / `g` / `h` для штрафов стоимости, бонуса материалов, штрафа зависимостей.
- [x] **D1-09** Реализовать расчёт **Feasibility Score** + ограничение диапазона score.
- [x] **D1-10** Реализовать `resolveFeasibilityStatus` в порядке приоритета блокировок (см. implementation §2.3).
- [x] **D1-11** Собрать результат `CalculateTaskFeasibility`: score, статус, причины, опционально кэшируемые поля для UI.

### Порты и заглушки

- [x] **D1-12** Добавить `IBudgetRepository` (чтение активного периода, лимит/остаток по принятой модели).
- [x] **D1-13** Добавить `IMaterialStockRepository` (список позиций, проверка наличия по id).
- [x] **D1-14** Добавить `NoopBudgetRepository` / `NoopMaterialStockRepository` для сборки без данных (или фиктивные значения — задокументировать).

### Юнит-тесты (фаза 1)

- [x] **D1-15** Тесты формулы: кейсы «септик» / «траншея», границы 1–5, нулевой бюджет, нет зависимостей.
- [x] **D1-16** Тесты `resolveFeasibilityStatus`: цепочка зависимостей, нехватка денег, нехватка материалов, `completed`.
- [x] **D1-17** Тесты `TaskDependencyValidator`: цикл, несуществующий id, ссылка на себя.

---

## Фаза 2 — Персистентность (Hive, data)

### Сущности и хранилища

- [x] **D2-01** Добавить `BudgetPeriodEntity` и `MaterialStockItemEntity` в domain.
- [x] **D2-02** Спроектировать ключи/боксы Hive (`budget_periods`, `material_stock` или единый бокс с префиксами).
- [x] **D2-03** Реализовать кодеки/DTO и мапперы domain ↔ storage (без импорта UI).
- [x] **D2-04** Реализовать `BudgetRepositoryImpl` и `MaterialStockRepositoryImpl`.
- [x] **D2-05** Расширить `TaskEntityCodec` (или эквивалент): новые поля + **значения по умолчанию** для старых записей.
- [x] **D2-06** Ввести/обновить `schemaVersion` в map задачи (или принятый в проекте способ миграции).
- [x] **D2-07** Прогнать ручную проверку: старые задачи открываются, новые поля не ломают список.

### Синхронизация

- [x] **D2-08** Зафиксировать решение: бюджет/склад только локально на MVP — без записей в sync queue; описать в implementation при изменении.

### Тесты (фаза 2)

- [x] **D2-09** Юнит-тесты миграции map: старая запись → новая с дефолтами.

---

## Фаза 3 — Use cases списков и интеграция с задачами

- [ ] **D3-01** Реализовать `CalculateTaskFeasibility` как оркестратор (задача + контекст + репозитории + калькуляторы).
- [ ] **D3-02** Реализовать `ListTodayReadyTasks`: только `ready`, сортировка по score, параметр лимита (например 3).
- [ ] **D3-03** Реализовать `ListBlockedTasksWithReasons`: не `ready`, не `done` (уточнить правило отображения), сортировка.
- [ ] **D3-04** Не раздувать `ITasksRepository`: при необходимости отдельный `ITaskDecisionQuery` / сервис на доменных типах.
- [ ] **D3-05** Связать с текущим экраном списка задач: не ломать существующий поток (фича-флаг / отдельная вкладка — по продукту).

### Тесты (фаза 3)

- [ ] **D3-06** Интеграционный или крупный unit-сценарий: две задачи, зависимость, смена `completed` у предшественника → задача попадает в выборку «Сегодня».

---

## Фаза 4 — UI, навигация, локализация

### Навигация и DI

- [x] **D4-01** Добавить сегменты путей в `app_route_paths` (типизированные роуты по правилам проекта).
- [x] **D4-02** Зарегистрировать маршруты в `auto_route` (вложенный стек или табы — по выбранному UX).
- [x] **D4-03** Подключить репозитории бюджета/склада и use cases в `AppScope` / scope фичи (конструкторная инъекция).

### Экраны

- [x] **D4-04** Экран **«Сегодня»**: сводка, топ-N (по умолчанию 3), «Показать ещё», карточка задачи (title, срок, стоимость, опционально score).
- [x] **D4-05** Экран **«Заблокировано»**: группировка или бейджи, развёрнутая причина из результата use case.
- [x] **D4-06** Экран **«Ресурсы»**: редактирование бюджета периода и списка материалов.
- [x] **D4-07** Elementary WM + model для каждого экрана (или согласованное разбиение без нарушения SRP).
- [x] **D4-08** Разделение mobile/desktop виджетов, если в проекте так принято для экранов.

### Локализация

- [x] **D4-09** Добавить ключи `tasks.decision.*` (заголовки, пустые состояния, причины блокировок с плейсхолдерами).
- [x] **D4-10** Обновить все языковые JSON и сгенерировать артефакты локализации по процессу репозитория.

### Доступность и полировка

- [x] **D4-11** Семантика / контраст / длинные тексты объяснений (переносы, не обрезать критичное).
- [x] **D4-12** Режим отладки: при `kDebugMode` логировать score и статус (без шума в release).

---

## Фаза 5 — Расширения (после MVP UI)

### Разбиение задачи

- [ ] **D5-01** `SuggestTaskSplit`: пороги по `complexity` / `estimatedCost`, возврат структуры подсказок.
- [ ] **D5-02** MVP UI: кнопка «Сложная задача» → диалог/экран с рекомендациями (без авто-создания подзадач).
- [ ] **D5-03** v2: мастер создания цепочки подзадач с зависимостями (после появления модели parent/child, если планируется).

### Альтернативы

- [ ] **D5-04** Кнопка «Подобрать альтернативу»: топ-K из `ready` по `listId` / тегам / близости score.
- [ ] **D5-05** v2: порт под внешние правила/сервис (без вызовов из WM напрямую).

### Настройки

- [ ] **D5-06** (Опционально) Экран или секция настроек: пользовательские веса формулы с валидацией и сбросом к дефолту.

---

## Кросс-срезы (по необходимости на любой фазе)

- [x] **DX-01** Обновить [DECISION_SYSTEM_IMPLEMENTATION.md](./DECISION_SYSTEM_IMPLEMENTATION.md), если изменились решения по синку, схеме Hive или UX.
- [ ] **DX-02** Widget/golden-тест карточки «заблокировано» с длинным текстом (опционально).
- [ ] **DX-03** Замер производительности пересчёта на N задач; при необходимости — кэш score в entity или инвалидация по событиям.

---

## Сводка по фазам

| Фаза | Идентификаторы задач |
|------|----------------------|
| 1 | D1-01 … D1-17 |
| 2 | D2-01 … D2-09 |
| 3 | D3-01 … D3-06 |
| 4 | D4-01 … D4-12 |
| 5 | D5-01 … D5-06 |
| Кросс-срезы | DX-01 … DX-03 |
