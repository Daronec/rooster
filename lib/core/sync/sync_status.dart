/// Агрегированное состояние синхронизации для UI и логов.
enum SyncEngineStatus {
  /// Нет сети или принудительная пауза (dev).
  idleOffline,

  /// Очередь пуста, сеть есть.
  idleSynced,

  /// Выполняется отправка операций.
  syncing,

  /// Есть ошибки у операций в очереди.
  degraded,
}
