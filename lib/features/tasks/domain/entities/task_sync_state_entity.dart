/// Локальное состояние синхронизации с облаком.
enum TaskSyncStateEntity {
  /// Только на устройстве.
  localOnly,

  /// Ожидает отправки.
  pendingSync,

  /// Подтверждено сервером.
  synced,

  /// Конфликт (требует разрешения политикой LWW / версиями).
  conflict,
}
