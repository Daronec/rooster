import 'package:flutter/widgets.dart';

/// Заглушка без `dart:io` (см. [profileLocalAvatarImage]).
Widget? profileLocalAvatarImage({
  required String? localFilePath,
  required double size,
}) => null;
