import 'package:elementary/elementary.dart';

/// Модель настроек (расширяется: шрифт, уведомления, обратная связь).
final class SettingsScreenModel extends ElementaryModel {
  /// Создаёт модель.
  SettingsScreenModel();
}
