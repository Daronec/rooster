import 'package:rooster/features/navigation/app_router.dart';

/// Мобильный роутер (совпадает с [AppRouter]).
class AppMobileRouter extends AppRouter {
  /// Создаёт мобильный роутер.
  AppMobileRouter({super.navigatorKey});
}
